<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.zin.rashidi.android.fugumod"
    android:versionCode="12"
    android:versionName="1.6.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:allowBackup="true">
        <activity
            android:name=".FuguModActivity"
            android:label="@string/title_activity_fugu_mod" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.support.v4.app.FragmentActivity" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".DownloadActivity" android:label="@string/app_name"></activity>
        <activity android:name=".FlashActivity" android:label="@string/app_name"></activity>
    </application>

</manifest>